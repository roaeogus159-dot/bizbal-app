# 인수인계서 — 비즈발 도안 생성기 (2026-07-10)

## 1. 프로젝트 개요
- **무엇**: 사진 → 비즈발(구슬발) 도안 변환 + 색상별 개수 계산 + 도안 3종 저장, **아이폰 우선 PWA**
- **위치**: `C:\Users\roaeo\task\비즈발 참고자료\code` (git 저장소, main 브랜치)
- **정식 사양**: 상위 폴더 `..\plan.md` (구현은 이 사양을 따름)
- **스택**: React + TypeScript + Vite, zustand, Canvas 2D, Web Worker, vite-plugin-pwa. 백엔드 없음.

## 2. 현재 상태: P1~P4 전체 구현 완료 ✅
plan.md 10장 로드맵 기준:
- **P1 (MVP)**: 사진 선택 → 초기 크기 2모드(가로 80cm / 총 9,000개, 비율 고정) → ΔE2000 매칭 → 색상 개수표(실제사진 토글) → 원형 비즈 미리보기(핀치줌/패닝) → 컬러 PNG 저장. 지름 4/6/8mm 기억. ✅
- **P2**: 에디터(점/영역브러시/같은색/스포이드, 다중선택, 전체선택/해제, 색교체, undo/redo, 초기화, 길게눌러 돋보기) + 전문가 모드(ΔE 초과 칸 주황 강조 + 임계값 슬라이더 + 대체색 추천) + 인쇄 A4(순번+범례+5/10칸선+4변 좌표+타일 분할) + 세로 줄 순서표(+줄 길이·편차) + 각각/모두 저장 ✅
- **P3**: 85색 전체 카테고리, 색상 라이브러리(검색·일괄·카테고리 토글), 커스텀 색 추가(재질·실제지름·사진), 재질감 미리보기(불투명/투명/반투명/오로라 + 배경 3종), 최대 색 수·디더링 ✅
- **P4**: 자동저장·최근 작업 이어하기(localStorage, ≤20만 칸), 데스크톱 2단 레이아웃 ✅

### 검증된 것 (실측)
- 총 개수 모드: 샘플(1080×1352) → 85×106 = 9,010개 ≈ 9,000 ✓
- 가로 cm 모드: 80cm·8mm → 100칸, 완성 80.0cm 표기 ✓
- 개수표 합계 = W×H (10,000 = 100×100 확인) ✓
- 300×300(9만 칸): 변환 ~1초, 디더링 포함 1.7초, 팬/줌 60fps ✓
- undo/redo/같은색 선택(202칸)/색교체/초기화 동작 ✓
- 전문가 강조(임계값 6 → 2,110칸 표시) ✓
- 인쇄 A4: 100×125 → 4장 타일 + 범례 1장, 4변 좌표·5/10칸선 ✓
- `npm run build` 통과 (PWA precache 100항목) ✓
- 데스크톱(≥900px) 2단 레이아웃 ✓

### 미검증 (실기기 필요)
- **아이폰 실기기**: Web Share로 사진앱 저장, 홈화면 추가, 오프라인, 핀치 제스처 감도, 돋보기 UX
- 실물 비즈와 HEX 대조 (plan.md: 화이트/아이보리/오로라 보정 권장)
- 배포(Vercel/Netlify)는 사용자 계정 필요해서 미실행 — 절차는 설명서에 정리됨

## 3. 파일 구조 (핵심)
```
code/
├─ src/
│  ├─ lib/color.ts        # sRGB→Lab, CIEDE2000 (직접 구현 — 이유는 §5)
│  ├─ lib/palette.ts      # 85색 로드, 커스텀 병합, enabled 인덱스
│  ├─ lib/convert.ts      # 이미지 디코드(≤1600px), autoSize, 워커 래퍼(최신 요청만)
│  ├─ lib/pattern.ts      # 개수표/순번(개수 내림차순), 줄 런렝스, 줄 길이(sizeMm 합)
│  ├─ lib/render.ts       # 비즈 스프라이트(플랫/재질), 오버뷰 비트맵, 뷰포트 컬링 drawGrid
│  ├─ lib/export.ts       # 컬러PNG / A4 300DPI(타일+범례) / 줄순서표 / Web Share 저장
│  ├─ workers/convert.worker.ts  # 박스 다운샘플→매칭→최대색→FS 디더링 (TypedArray)
│  ├─ state/store.ts      # useSettings(persist) + useProject(메모리+자동저장)
│  ├─ screens/ Home Convert Editor Result Library
│  └─ components/ PreviewCanvas(핀치줌/브러시/돋보기) ColorList PaletteSheet BeadSwatch
├─ public/beads/{code}.jpg  # 실제 비즈 사진 85장 (../assets/beads에서 복사)
├─ public/sample.jpg        # 샘플(체험·데모·캡처용)
├─ Output/비즈발_도안생성기_사용설명서.html  # 실제 스크린샷 임베드 설명서
├─ Reference/capture.mjs    # 화면 캡처 스크립트 (playwright, 시스템 Chrome)
├─ Reference/screenshots/   # 캡처본 10장
└─ Handover document/       # 이 문서
```

## 4. 실행·배포·캡처 방법
- **실행**: `npm install` → `npm run dev` (또는 `.claude/launch.json`의 bizbal-dev, 포트 5199)
- **빌드**: `npm run build` → dist/
- **배포**: GitHub push → Vercel Import → Deploy (설명서 3장에 단계별 정리)
- **화면 캡처 재생성**: dev 서버 켠 상태에서 `node Reference/capture.mjs`
- **데모 URL**: `?demo=convert|editor|result|library` → 샘플 자동 로드 후 해당 화면 이동 (검증·캡처용)
- **설명서 재생성**: 스크래치패드의 make_manual.py 참고 (스크린샷 → base64 임베드 HTML)

## 5. 설계 결정·주의사항
- **색 연산을 culori 대신 직접 구현**: 핫루프(수만 칸 × 85색 ΔE2000)에서 객체 할당 없는 순수 숫자 함수가 필요했음. `lib/color.ts`에 Sharma 기준 CIEDE2000. 검증된 표준 공식이며 워커에서 RGB 캐시와 함께 사용.
- **grid는 전체 팔레트 인덱스(Uint16)**: 팔레트 배열 = 기본 85색 + 커스텀(추가 순). **커스텀 색 삭제 시 자리 유지(deleted 플래그)** — 인덱스가 밀리면 기존 grid가 깨지기 때문. UI에서만 숨김.
- **미리보기 성능**: 칸<2.5px 또는 가시 칸>4만이면 1칸=1px 오버뷰 비트맵을 스케일 드로우, 그 외엔 (색×재질×크기) 스프라이트 프리렌더 후 스탬프.
- **재변환 시 수동 수정 소실** → 크기/팔레트 변경 전 window.confirm 경고 (plan.md 3.3 정책).
- **자동저장**: 20만 칸 초과 grid는 저장 안 함(localStorage 5MB 한계). 복원 시 cellRgb/deltaE 없음 → 전문가 강조는 재변환 후 표시됨.
- **iOS 저장**: navigator.canShare({files}) 지원 시 공유시트(사진 저장), 아니면 다운로드 폴백(350ms 간격 순차).
- **알려진 버그 수정 이력**: decodeImage에서 bmp.close() 후 bmp.width 참조로 aspect가 항상 1이 되던 버그 수정됨 (convert.ts).

## 6. 남은 작업 후보 (다음 세션)
1. **아이폰 실기기 QA** (저장·제스처·오프라인) — plan.md 11장 체크리스트의 iOS 항목
2. GitHub 원격 연결 + Vercel 배포 (사용자 계정 필요)
3. 실물 대조 HEX 미세 보정 (특히 화이트/아이보리/오로라)
4. 선택: 인쇄 PDF 옵션(jspdf), 프로젝트 다중 저장 슬롯, 줄 순서표 텍스트(.txt) 내보내기
5. plan.md §4.1의 (옵션) 항목: 축소 전 대비/채도 보정, k-means 색수 축소(현재는 사용빈도 상위 제한 방식)

## 7. 데이터·에셋 출처
- `src/data/euncen_palette.json` = `..\euncen_palette.json` 복사본 (은센 85색)
- `public/beads/*.jpg` = `..\assets\beads` (crop_beads.py로 원본 색상표 스크린샷에서 추출, 이미 완료 상태였음)
- `public/sample.jpg` = `..\KakaoTalk_20260709_002635505_01.jpg`

## 8. 추가 업데이트 (2026-07-10 오후)
- **전문가 모드 가시성**: 변환 화면에 '변경 권장 N칸' 배지 + ΔE 임계값 슬라이더, 축소 시 반투명 주황 채움
- **원본 사진 오버레이**: 변환·에디터 미리보기 우상단 [원본 ON/OFF] + 투명도 슬라이더(10~90%). PreviewCanvas가 image.rgba로 원본 캔버스를 만들어 격자에 맞춰 합성
- **목표 가로/총 개수 1초 자동 적용**: 입력 후 1초 디바운스로 applyAutoSize (Convert.tsx, 수정 있으면 confirm)
- **완성 크기 표기 줄바꿈** (총 개수 / 크기 2줄)
- **구매 계획 + 엑셀(CSV) 추출**: lib/purchase.ts + components/PurchasePlan.tsx. 은센 기준 100개입 1,000원(상품페이지 2026-07 확인), 배송비 3,000원/3만원 이상 무료. packSize·카테고리별 단가는 useSettings에 영속(packPrices). CSV는 UTF-8 BOM+CRLF(엑셀 한글 정상)
- **공유시트 판별 수정**: navigator.share를 모바일 UA에서만 사용(shouldUseShare) — 데스크톱 Chrome에서 다운로드가 OS 공유창에 막히던 문제 해결 (이미지 저장·CSV 공통)
- **사진 폴백**: BeadSwatch가 실제 사진 로드 실패 시 색상 원으로 자동 대체
- **단계별 말풍선 가이드**: lib/guides.ts(화면별 스텝 데이터) + components/GuideTour.tsx. 헤더 뒤로 버튼 오른쪽 [💬 가이드] 버튼. 대상 요소는 data-guide 속성으로 지정, 자동 스크롤+금색 링 강조+4분할 딤. 말풍선: 좌하 [가이드 종료], 우하 [이전][다음(마지막엔 완료)]. 화면 전환 시 자동 닫힘. 변환 9·에디터 6·결과 5·라이브러리 5단계.
- **파스텔 핑크 테마 전환**: index.css :root 변수(--primary #d9628d, --primary-soft #fce7ef 등) + 헤더 #fce8f0 + 홈 그라데이션. PWA theme_color/아이콘(#F9CFDF 배경)도 교체(make_icons.py 스크래치패드). 선택 오버레이(파랑)·전문가 강조(주황)·가이드 링(금색)은 기능색이라 유지.

## 9. 배포 (2026-07-10)
- **주소**: https://roaeogus159-dot.github.io/bizbal-app/
- **방식**: GitHub 공개 저장소 `roaeogus159-dot/bizbal-app` + GitHub Actions → Pages (main push 시 자동 재배포)
- **서브경로 대응**: vite.config `base: process.env.DEPLOY_BASE`(CI에서 `/bizbal-app/`), 코드 내 에셋 경로는 `import.meta.env.BASE_URL` 기준 (절대경로 `/beads/...` 쓰면 배포에서 깨짐 — 주의)
- **검증 완료**: 변환 동작, 비즈 사진 로드, 서비스워커 등록(오프라인), 콘솔 에러 없음
- Vercel/Netlify로 옮기려면: 같은 저장소 Import만 하면 됨 (DEPLOY_BASE 미설정 시 루트 배포라 그대로 동작)
- **에디터 개선(여자친구 피드백)**: ① [💾 중간 저장] 버튼(doAutosave 즉시 실행+토스트) ② 사용 중인 색상 바(.used-strip, 개수순, 탭=선택 교체/현재 색 지정) ③ '영역'→'칠하기': strokePaint/strokeCommit로 드래그 중 즉시 반영, 손 떼면 undo 1개로 확정(핀치 시작 시에도 커밋) ④ 초기화를 undo 가능한 diff 엔트리로 변경. 되돌리기 비활성 원인이던 '동일 색 적용=무변화'와 '초기화 후 스택 삭제' 해결.
- **에디터 개선(여자친구 피드백, PC 기준 검증)**: ① [💾 중간 저장](doAutosave 즉시+토스트) ② 사용 중인 색상 바(.used-strip, 탭=선택 교체/칠하기 색 지정) ③ '영역'→'칠하기': strokePaint/strokeCommit — 드래그 중 즉시 반영, 손 떼면 undo 1개로 확정 ④ 초기화도 undo 가능한 diff 엔트리. 되돌리기 비활성 원인('같은 색 적용=무변화', '초기화 후 스택 삭제') 해결. ※ 검토·확인은 PC(1280×800) 우선으로 진행할 것(사용자 요청).
- **칠하기 되짚기 취소(스크럽 백)**: strokeMove가 경로(strokePath) 순서를 유지 — 직전 경로로 되돌아가면 해당 칸을 원래 색으로 복원(자기 경로 '교차'는 유지). PreviewCanvas는 이동 샘플 사이를 Bresenham 보간해 순서대로 전달(빠른 드래그에도 끊김 없음). PC 픽셀 검증: 칠하기 전[196,203,213]→칠함[116,143,176]→되짚음[196,203,213] 원복 확인.
- **색상 바 최근 사용 순 + 직접 채우기 모드**: ① recentColors(MRU, useProject)로 방금 고른 색이 색상 바 맨 앞. 색상 바에 [＋](팔레트에서 색 고르기)·[∅](지우개) 고정 버튼 추가, 색만 고르면 칠하기 도구 자동 전환 ② 채색 모드 '직접'(paintMode='manual'): EMPTY(0xFFFF) 센티널로 빈 칸 격자 생성, 고스트 링 렌더, buildLegend/개수표/구매계획은 빈 칸 제외("채움 N개" 표기), 인쇄는 빈 칸 공백, 줄순서표는 '빈칸×n'. 지우개=EMPTY 칠하기. PC 픽셀 검증 완료.
- **내 작업 목록 + 작업 파일 이동(2026-07-17)**: ① lib/db.ts(IndexedDB 'bizbal'/projects) — doAutosave가 projectId 단위로 upsert, 새 사진=새 작업. screens/Projects.tsx: 도안 썸네일·열기·이름변경·📤내보내기·삭제 ② lib/project.ts(.bizbal.json v1): dataUrl+gridB64+codes(인덱스→코드표)+customDefs. 불러올 때 코드 기준 재매핑, 없는 커스텀 색은 자동 추가 → 카톡/메일/USB로 다른 컴퓨터 이동 가능. 홈에 [내 작업 목록]/[작업 파일 열기] 버튼. PC 검증: 자동저장→목록→내보내기→저장소 초기화(타 컴퓨터 시뮬)→파일 열기→재저장 전 과정 통과.
- **다크 모드**: useSettings.theme('light'|'dark', 영속) → html[data-theme] + meta theme-color 동기화(App.tsx). 색상은 전부 CSS 변수화(:root 라이트 / :root[data-theme='dark'] 플럼 톤 오버라이드). 토글 버튼은 텍스트 표기([🌙 다크]/[☀️ 라이트]) — 헤더 우측 + 홈 우상단 플로팅. 도안 캔버스 배경은 비즈 색 판독을 위해 중립 유지.
- **중간저장 수정 보존 버그 수정 + 재변환 선택 모달(2026-07-17)**: 원인=Convert 마운트 시 무조건 requestConvert()가 복원된 세부수정을 덮어씀. 해결: ① convertedKey(currentConvertKey)로 설정이 실제로 바뀐 경우만 재변환 ② hasEdits()를 grid≠baseGrid 비교로 변경(복원 후 undo스택 비어도 수정 인식) ③ 저장 시 baseGrid도 함께 저장(db.ts base, localStorage base) → 복원 후에도 수정 칸 구분. 설정 변경 시 자동 재변환 대신 3버튼 모달: [세부 수정 사항 유지]=requestConvert(true, 수정 칸 복원+나머지 재변환), [세부 수정 사항 포함]=requestConvert()(전부), [취소]=declinedKey로 무시. 색상 바도 '내가 고른'/'자동 변환' 그룹 구분. PC 픽셀 검증: 이어하기 보존·모달·유지 재변환 모두 통과.
- **올가미(자유형) 영역 선택(2026-07-17)**: 새 도구 'lasso'(➰ 영역 선택). 즉시 채우기가 아니라 "그린 자유형 영역 안쪽 칸을 선택"하고, 이후 색상 바/[색 교체]로 채우는 방식(사용자 요청). PreviewCanvas가 드래그 경로를 CSS px로 기록→점선+반투명 오버레이 표시, 손 떼면 셀 좌표 float 다각형으로 변환해 onLasso. lib/geom.ts enclosedCells(홀짝 ray-casting, bbox 한정)로 내부 칸 계산→selection에 union. 벌어져도 끝점 자동 연결로 채움. PC 검증: 원형 올가미→930칸 선택→BB 채우기→선택 해제·되돌리기 활성 확인.
- **영역 채우기로 변경(2026-07-18)**: 올가미를 '선택'→'즉시 채우기'로. 도구명 '영역 채우기'(🪣). 색상 바에서 현재 색 고른 뒤 자유형으로 영역을 빙 둘러 그리면 손 떼는 즉시 enclosedCells 내부를 currentColor로 applyColor(한 번의 되돌리기). 색 미선택 시 토스트 안내 후 차단. 의도: 큰 영역을 칠하기로 일일이 안 칠하고 테두리만 그려 빠르게 채움. PC 검증: 미선택 차단·선택 후 734칸 즉시 채움·선택0 유지·되돌리기 활성.
- **채우기를 그림판식 플러드 필로 변경(2026-07-18)**: 도구 id 'lasso'→'bucket'(🪣 채우기). 자유형 그리기 제거, lib/geom.ts enclosedCells→floodFillRegion(4방향 BFS). Editor onCellTap의 bucket 분기: 색 고른 뒤 탭하면 그 칸과 이어진 같은 색 영역을 currentColor로 applyColor(한 번의 되돌리기). 색 미선택 차단, 같은 색이면 무시. onStripTap/applyColor 도구전환 가드에 bucket 예외(색 골라도 채우기 유지). PreviewCanvas 올가미 코드 전량 제거, bucket은 탭 도구. PC 검증: 색차단·직접모드 빈 도안 12,500칸 채움·되돌리기 원복.
- **버그 점검·수정(2026-07-18)**: 전 화면 PC/모바일 회귀 테스트(콘솔 에러 0). 발견·수정: ① currentConvertKey — 직접(manual) 모드는 ['manual',W,H]만 사용해 색수·디더링 변경 시 뜨던 불필요한 재변환 모달 제거, 커스텀색 키를 개수→코드목록으로(삭제+추가 시 미감지 방지) ② 모바일(≤430px) 헤더 제목 줄바꿈 해소(nowrap+ellipsis, 버튼 여백 축소). 그 외 저장/복원·undo/redo·채우기·재변환모달·작업목록·다크모드 정상 확인.
- **실측 지름 반영 + 행·열 편집(2026-07-18)**: ① pattern.ts actualBeadMm(diameterMm, finish): 8mm 불투명·반투명 7.6/투명·오로라 8.0, 6mm 5.6/5.8, 4mm=표기값. strandLengths가 이를 사용(커스텀 색은 입력 sizeMm, 빈 칸=0). Result·export(renderStrandSheets)에 diameterMm 전달. 완성 크기 헤드라인(finishedSizeCm)은 표기값 유지. ② store.cropGrid(edge,delta): 가장자리 행/열 추가(빈 칸)·자르기, grid/baseGrid remap, cellRgb·deltaE·undo 초기화, convertedKey=새 크기로 갱신(재변환 안 함). 세부 수정 화면 '✂️ 행·열 자르기/추가' details 카드(위/아래/왼/오 ±). 자른 줄은 undo 불가(반대쪽 추가로 빈 줄 복원). PC 검증: 실측값 정확, 100×125→98×128 유지·재변환 모달 없음, 콘솔 에러 0.
- **행·열 자르기/추가 되돌리기 지원(2026-07-18)**: UndoEntry를 discriminated union으로 확장 — CellEdit(kind:'cells') + ResizeEdit(kind:'resize', 전/후 grid·base·W·H 스냅샷). cropGrid가 undo 스택을 비우지 않고 resize 엔트리 push. undo/redo가 kind로 분기(resize는 크기·격자 스냅샷 복원 + convertedKey 갱신). LIFO 불변식 덕에 cells 엔트리가 스택 top일 때 현재 크기 = 그 편집 시점 크기로 항상 일치(그 뒤 크롭은 먼저 undo됨). PC 검증: 자르기 undo/redo, 색칠↔크롭 혼합 undo×3/redo×3 크기·색 정확 복원, 콘솔 에러 0.
- **애플펜/손가락·마우스 좌우 입력 분리(2026-07-19)**: PreviewCanvas 포인터 역할(edit/pan) 분류 — pen=edit, mouse 좌=edit·그외=pan, touch는 penMode면 pan(아니면 edit). role='pan'은 도구 미적용·드래그로 팬만. 핀치는 touch 2개일 때만(펜+손가락 오작동 방지). onContextMenu 억제(우클릭 팬). useSettings.penMode(persist, 기본 false) + 편집 캔버스에서 펜 첫 감지 시 자동 ON + onAutoPen 토스트. Editor에 '애플펜 모드' 토글·안내 문구. setPointerCapture try/catch 가드. PC 합성포인터 검증: 좌클릭 편집/우클릭 팬/펜 편집/손가락(펜모드) 팬·탭무편집/자동감지/핀치줌 모두 통과, 콘솔 에러 0.
- **행/열 이동·복사/붙이기·툴바 2줄·카톡 공유(2026-07-19)**: ① store.applyCellValues(cells, values) — 칸별 다른 값 undo 1회 적용(공용) ② 행/열 이동(rowcol 도구): 탭→십자 강조(render.ts DrawOptions.cross)+패널 ▲▼◀▶로 인접 줄과 스왑, 대상이 따라가 연타 이동, undo 지원 ③ 복사/붙이기(paste 도구): 선택 칸→bounding box 상대좌표 클립보드(EMPTY 포함)→탭 중앙 기준 배치·반복·undo ④ 툴바 A안: .tool-row grid 4열×2줄(스크롤 제거) ⑤ 카톡: project.ts shareProjectFile — 모바일 공유시트(navigator.share, .bizbal.json File) 우선/PC 다운로드 폴백, Projects 안내문(카톡 파일→'파일에 저장'→[작업 파일 열기]) 갱신. PC 검증 전항목 통과(툴바 8개·2줄·noScroll, 십자강조·스왑·undo 기준해시 일치, 붙이기 토스트·undo, 다운로드 폴백), 콘솔 에러 0. Reference/toolbar_mockups.html=툴바 시안 목업.
- **격자 보기 토글(2026-07-19)**: 미리보기 우상단 [🖼 원본] 아래 [🔢 격자] 버튼(useProject.chartOn). render.ts DrawOptions.chart={num: 순번맵} — 사각 칸 채움+순번(s≥14, isLight로 흑/백), 매 칸/5칸/10칸 선(전역 좌표 기준, s<7이면 얇은 선 생략), 상단·좌측 가장자리 좌표(1·5의 배수, s≥10 아니면 10의 배수, 흰 halo). PreviewCanvas가 chartOn 시 buildLegend→legendNumberMap 계산(개수표·인쇄와 동일 번호), 돋보기에도 적용. 원본 오버레이와 동시 사용 가능. 휠 줌을 non-passive 리스너로 교체(preventDefault 경고 제거). PC 검증: 토글·줌 순번·동시사용·콘솔에러 0.
